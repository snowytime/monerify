# 💰 Monerify (in progress)

This is a nodejs version of the api to interact with the moneris payment gateway in Canada. This api is meant to be used server side only, handles most of the available actions of the api, and handles all the xml construction and parsing that is required by the api.